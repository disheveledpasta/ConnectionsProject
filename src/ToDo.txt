1. Finish first prototype, then analyze what to do. Likely involving the interface, more methods, and data persistence. <-- after current

2. Reform commands list

3. Add editProperty(), editConnection(), and editConnectionProperty(). <-- DONE

4. Add a bunch of null checkers. <-- DONE

5. Maybe make it so if ID Cards are removed, the next ID Card created can replace the now empty ID Number. Weigh benefits and drawbacks.

6. Reform variable names in the switch statement in IdentityCardManager <-- DONE

7. Add an option for reciprocated connections.

_____Groups_____

1. Fix nextGroupNumber so it actually increments.

2. Make it so nextIDNumber is per-group rather than per-program.